name: 每日更新行情数据

on:
  schedule:
    # 收盘后完整跑（UTC 21:30 = 北京 5:30）
    - cron: "30 21 * * *"
    # 亚洲时段每30分钟：韩日 8:00 开盘起 至 A股/港股收盘（UTC 0-7 = 北京 8:00-15:37，工作日）
    # 用 :07/:37 错峰，避开 GitHub cron 整点拥堵被跳过
    - cron: "7,37 0-7 * * 1-5"
    # 美股交易时段每30分钟（UTC 13:30-20:00，工作日）
    - cron: "7,37 13-20 * * 1-5"
  workflow_dispatch: {}   # 支持手动触发

concurrency:
  group: update-data
  cancel-in-progress: false

permissions:
  contents: write

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: 拉取行情并计算指标
        run: python scripts/update_quotes.py

      - name: 提交更新
        run: |
          git config user.name "quotes-bot"
          git config user.email "actions@github.com"
          git add data/quotes.json
          git diff --cached --quiet || git commit -m "chore: 更新行情数据 $(date -u +%F)"
          git pull --rebase origin main
          git push
