# CLAUDE.md — 项目交接手册

> 给任何接手本项目的 Claude（新会话 / Claude Code）：读完本文件即可开始维护，无需之前的对话记录。

## 项目是什么

「烟囱的美股学习笔记」：主人 yancong 的个人投资数据面板 + 学习笔记分享站。

- 线上地址：https://chenyanchong321.github.io/us-stock-notes/
- 仓库：github.com/chenyanchong321/us-stock-notes（Public，GitHub Pages 从 main 分支根目录发布）
- 两个栏目：📊 数据面板（19 个板块 ~270 个标的行情）+ 📚 学习笔记（网页文章 + PDF 下载）

## 架构（三层分离）

| 文件 | 作用 | 谁改 |
|---|---|---|
| `config/watchlist.json` | 关注池配置：板块、标的、Yahoo代码、市值基准 | 加/删股票改这里 |
| `config/notes.json` | 笔记卡片目录 | 加笔记改这里 |
| `data/quotes.json` | 行情数据（**自动生成，勿手改**；除非重排板块需同步顺序） |
| `scripts/update_quotes.py` | 数据抓取：Yahoo chart API，每标的两路（5y日线算涨跌 + max月线补历史高点），复权价口径 |
| `.github/workflows/update-data.yml` | 每天 UTC 21:30（北京 5:30）自动跑脚本、commit、触发 Pages 重发布；支持手动 Run workflow |
| `index.html` | 首页（读 JSON 渲染），一般不动 |
| `articles/*.html` | 文章页，白底黑字阅读版；新文章参考现有文件结构 |
| `notes/*.pdf` | 文章对应的 PDF 存档 |

## 高频操作 SOP

**加股票**：在 watchlist.json 对应板块 items 里加一条：
```json
{"name":"公司名","code":"显示代码","market":"美股/A股/港股/日股/韩国",
 "yahoo":"Yahoo代码","currency":"$",
 "mcap_base":{"prefix":"$","yi":市值亿数,"approx":true},"mcap_base_price":当时股价,"ath_floor":0}
```
Yahoo 代码规则：A股 `.SS`/`.SZ`，港股去前导0加 `.HK`（02476→2476.HK），日股 `.T`，韩股 `.KS`。
mcap_base 的本质是"股本数"：yi/mcap_base_price 应等于总股本（亿股），估算即可（页面已声明估算口径）。
改完 push，然后到 Actions 手动 Run「每日更新行情数据」让新标的立刻有数据。

**加板块**：sections 里加 `{"name":"NN 板块名","items":[...]}`，如需重排全站顺序，记得把 data/quotes.json 的 sections 同步重排（否则要等下次数据刷新才生效）。同一标的可跨板块重复（用同一配置对象即可）。A/H 两地上市相邻排列。

**加笔记**：文章 HTML 放 `articles/`（复制现有一篇改正文，白底黑字模板），PDF 放 `notes/`，在 notes.json 加卡片（t/date/tags/d/page/ts）。**发布时必须写 ts=「YYYY-MM-DD HH:MM」发布时刻**，时间线按 ts 倒序，缺 ts 同日排序会乱。

**推送方式**：需要主人提供 fine-grained PAT（仅限本仓库，Contents+Workflows 读写；有效期以主人创建时的设置为准，2026-07 主人确认设的是 90 天，勿再默认说 7 天），用
`git push https://x-access-token:TOKEN@github.com/chenyanchong321/us-stock-notes.git main`。
推送前先 `git pull --rebase`（机器人每天会 commit 数据）。

## 已踩过的坑（勿重蹈）

1. **Yahoo range=max&interval=1d 对老股票会悄悄降级/截断近期数据** → 已改为 5y日线+max月线双路，勿改回单路。
2. 必须用 **adjclose 复权价**，否则高分红股涨跌幅和行情软件对不上。
3. YTD 基准 = 当年 1 月 1 日前最近收盘（上年末），不是 12-31 00:00。
4. Pages 偶发 "Deployment failed, try again later" 是 GitHub 服务端故障，空 commit 重推即可。
5. 涨跌幅口径与富途基本一致；历史高点是**收盘价口径**（盘中高点会略高，属正常）。
6. 首页表格行数据是 15 列数组：[名称,代码,市场,市值,高点,现价,回撤,近1月,近3月,近半年,YTD,近1年,分组名,PE(TTM或null),52周位置0-100,52周区间字符串,当日涨跌%或null]。PE 走 Yahoo v7 quote（cookie+crumb），失败自动降级为空。

## 风格约定

- 数据面板深色主题，**绿涨红跌**；文章页白底黑字。
- 板块顺序 = 主人的重点动线：指数→七巨头→芯片→存储→光连接→云算力→供应链→其他资产，调整顺序需主人确认。
- 免责声明保留在页脚和文章尾部。
- 主人偏好：回复简洁、先讨论方案再执行大改动、数据准确性问题必须追根因。

## 数据面板二级分组机制（2026-07-06 增）

- 板块内二级分组：在 watchlist.json 的 section 加 `"groups":[{"name":"组名","codes":[代码...]}]`，items 顺序须与 groups 展开顺序一致（同一标的在一个板块内只能属一个组；跨板块重复不受影响）。
- update_quotes.py 会把组名写进每行第 13 列（r[12]）；本地手改分组后要同步重排 data/quotes.json 各行顺序并补 r[12]，否则要等下次数据刷新才生效。
- 前端自动渲染：表内蓝色分组标题行（锚点 secN-gM）+ 侧边栏缩进二级链接；搜索时分组行自动隐藏，排序时切平铺。
- 表头为 JS 悬浮实现（#floatHead，任意窗口宽度吸顶、随表格横滚同步），勿改回 CSS position:sticky（与横向滚动容器互斥）。
- 当前 18 个板块中 12 个已分组（光连接8组、800VDC5组、设备5组、材料5组、软件5组、能源4组、存储4组、芯片4组、云4组、贵金属4组、封测3组、光纤3组、中国互联网3组）。
- 板块合并史：碳化硅+800VDC→「800VDC 功率半导体（SiC·GaN）」；光芯片激光器并入光连接。

## 标的简介浮窗（2026-07-06 增，固定规则）

- `config/profiles.json`：每个标的代码对应一段 HTML 简介，鼠标悬停（手机点按）名称时弹出浮窗。
- **固定规则：以后每新增一个标的，必须同步在 profiles.json 手写一条简介**，缺了算任务没完成。
- 文案风格（C 记忆卡式）：`<b>粗体第一句</b>`一句话讲清它是干嘛的（能用类比就用类比，如「收费站」「全科医生」），后接 2-3 句自由发挥：地位数据、叙事逻辑、与同组邻居的区分。60-120字。人设绰号尽量沿用学习笔记体系（EML之王、泵浦之王、悲情先驱等）。
- A/H 两地上市共用同一条文案（两个代码各写一个 key，值相同）。
- 浮窗实现在 index.html 的「标的简介浮窗」IIFE：#stip 固定定位、跟随鼠标、自动避让屏幕边缘；名称上不加下划线不改光标（主人明确要求）。

## 买点标注机制（2026-07-06 增）

- `config/buypoints.json`：{代码:"点位文案"}，前端在「现价」下方渲染灰色小字「买点 xx」。没有配置的标的不显示，不占空间。
- 点位来源：主人的《投资机会和价格点位》总文档（桌面 金融投资/00_投资机会和价格点位/）。主人给出新买点时同步更新此文件即可，无需动数据管线。
- 列顺序（显示序）：名称/市值/历史高点/现价(+买点)/高点跌幅（52周位改为悬停现价弹出信息卡，不占列）/近1月.../PE。数据行索引不变（15列见上文），仅前端展示顺序调整。

### 买点书写铁律（2026-07-06 补）

- **买点必须写具体数值**，禁止"当前可分批"这类相对表述——现价会变，相对表述会误导。凡"按当前价分批"型建议，一律换算成「x.x以下分批（MM/DD锚）」并注明锚定日期。
- 有买点的标的前端自动高亮：名称前金色★+名称列金色左边条+买点文字金色（tr.buy 类）。

## 盘中准实时与新增机制（2026-07-06 增）

- **数据更新频率**：亚洲时段（北京8:00-15:37，含韩日开盘）与美股时段（UTC 13:30-20:00）每30分钟（用:07/:37错峰，GitHub cron整点常被跳过）自动跑一次 workflow，收盘后 21:30 UTC 完整跑一次。Public 仓库 Actions 免费不限量。concurrency 防止并发冲突。
- **买点触达**：前端解析 buypoints 文案里第一个数字为触发价，现价 ≤ 触发价×1.02 时买点文字变绿加🎯，并在顶部「已进买点区」聚合条汇总（含风险锚/市值类文案的不参与解析）。
- **相关笔记互链**：浮窗底部自动附相关笔记链接，映射表在 index.html 的 NOTE_CODE（按代码）与 NOTE_GRP（按分组名关键词）。新笔记发布后应视情况补映射。
- **手机端**：≤860px 时 header 紧凑化（logo 缩短、slogan 隐藏），topbar 出现「跳转板块」下拉。

## 前端实时行情层（2026-07-06 增）

- index.html「实时行情」IIFE：页面每30秒经 `qt.gtimg.cn`（腾讯行情，script 标签 JSONP 免 CORS，charset=GBK）直连刷新**现价与当日涨跌**，并重算买点触达聚合条。覆盖 A股(sh/sz)、港股(hk五位码)、美股(usTICKER)、日股(jp)、韩股(kr)、主要指数（映射表 IDX）；台股/OTC/商品/加密不覆盖，保持每日快照值。
- 分层职责：实时层管现价/当日/市值/回撤/52周位（锚点+现价实时推导）；GitHub Actions 管锚点与 PE/各周期涨跌（cron 亚洲8:07-15:37、美股21:37-4:07 每30分钟）（cron 已错峰 :07/:37）。腾讯接口若失效，页面自动退回每日快照，不会坏。

## 视图切换器（2026-07-07 增）

- 顶部统计卡即视图切换：全景（观察标的）/ ⭐重点清单（买点标的，含"距买点"列，触达置顶）/ 今日涨幅榜Top30 / 跌幅榜Top30 / 回撤榜(>30%) / 板块热力（当日中位涨跌·剔除ETF指数·含领涨直达与分化极差列）。
- 跨板块交叉引用标的在榜单里按代码去重；实时行情30秒刷新时自动重建当前榜单；搜索输入自动切回全景。
- 实现在 index.html renderDash 末尾的「视图切换器」段（buildFlat/setView），榜单为跨板块平铺表，带"板块"列。

## 市场全局过滤器（2026-07-07 增）

- 视图条「市场」卡：全部/A股/港股/美股/日股/韩股/台股。选中后全站生效：全景（复用 applyFilter，rowHTML 带 data-mkt）、五个榜单（flatRows 过滤）、板块热力、买点机会条。选择存 localStorage("mkt")。**切市场保持当前视图不变**（市场=滤镜，视图=镜头）；再点当前选中的视图卡=回全景。
- 匹配规则 MKT_RULE 按 market 字段前缀（"日"匹配日股/日本/日股 指数）。

## 术语词典与开盘状态（2026-07-07 增）

- 第三页签「术语词典」：config/glossary.json（{cats[], terms:[{t,f,c,d}]}），renderDict 支持搜索/分类/A-Z（中文开头归"中"）。**新增学习笔记涉及新黑话时应顺手补词条**，风格=白话+类比，与笔记体系一致。
- 市场下拉菜单每项显示 ●盘中/○休市 + 交易时段（北京时间），美股自动判夏令时（mktStatus 函数）；打开菜单时实时刷新状态。

## 超额收益（2026-07-07 增）

- 「超额榜」视图：个股 YTD/近1年 减去**对应市场基准**（A股→沪深300、港股→恒指、美股→标普SPX、日→日经、韩→KOSPI、台→TWII 台湾加权），锚必须匹配市场（丹萍原则）。悬停浮窗自动附「相对大盘 YTD ±pp」一行。benchOf/alphaOf 在 index.html。

## 列顺序与榜单一致性（2026-07-07 定版）
- 全站统一列序：名称/市值/现价/历史高点/高点跌幅/当日/近1月/近3月/近半年/年初至今/近1年。现价紧跟市值，历史高点紧挨高点跌幅（父子字段相邻）。
- 榜单视图（重点清单/涨跌幅榜/超额榜/回撤榜）表头与主页完全一致，不得省列；专属列（距买点/超额pp）紧跟现价之后。回撤榜不再单加"历史高点"extra列（已是标准列）。
- 52周位不占列：鼠标悬停任意视图的现价(td.pxc)弹信息卡（区间+位置条+距高低点），见 posTip()。
- 改列序牵连三处必须同步：rowHTML/fvRow、SORT_MAP、applyQuote 的 tds 下标。

## 双行单元格与港股基准拆分（2026-07-07）
- 市值列双行：上市值下小字 PE（mcapCell()）；PE 不再挂在现价括号里。
- 现价列双行：全景/涨跌幅榜/超额榜/回撤榜第二行=「52周 低–高」；重点清单第二行=买点（priceCell(r,"buy")）。浮窗保留更深信息（当前位%、距高低点）。
- 港股超额基准拆分（丹萍建议）：科技/新经济板块→恒生科技（3033 ETF代理），能源/电力/资源/价值老登/贵金属→恒指，见 benchOf(mkt,sec) 的 HK_TRAD 正则。风格错配与权重股自我对比两点不做工程化修正，只在超额榜说明里注明局限。

## 大事件日历（2026-07-07 上线）
- 页签「📅 大事件」：未来7天置顶（倒计时）+按月时间轴+45天内已过事件可回看；类型色标 财报🟦/宏观🟥/上市🟨/产品🟩；关联标的点击跳数据面板行并闪烁（jumpToCode）。数据面板 #evweek 显示"本周大事 N 件"直达。
- 数据两层：①自动层 data/events.json——update_quotes.py 在 PE 批量请求里顺带取 earningsTimestamp（零额外请求），留未来120天，估算日期标 est；②人工层 config/events.json——宏观官方日程（FOMC/CPI/非农，每年初我更新一次，2026已填到年底）+特殊事件（IPO等，对话提到随手补）。用户不提供数据。
- 铁律：对话中出现新的重要事件（上市/发布会/政策节点），主动补进 config/events.json。

## 四项更新（2026-07-07 傍晚）
- 回撤配色对调（用户：黄牌先于红牌）：距高点跌幅>30% 红色加粗(.ddsev)，≤30% 黄色(.ddmild)，全站统一。
- 新高榜视图 "hi"：回撤<10% 的标的，按距新高由近到远；与回撤榜互为镜像。
- 手机端顶栏两行：--header-h 移动端 94px，第一行 Logo+搜索，第二行四页签横排可滑（.tabs order:3 width:100%）。
- 美股盘前/盘后（最终方案，2026-07-07 实测定案）：腾讯网页源美股为延时冻结口径（qt块标"delay"，f[9]/f[19]是收盘冻结盘口，不是实时，勿再踩坑）。改由流水线 Yahoo v7 quote 顺带取 preMarketPrice/postMarketPrice（零额外请求）写入行数据第18元素 r[17]={px,pct,st}；cron 补 UTC 8-12（北京16-21盘前）与 UTC 21-23（北京05-08盘后深段）。前端 applyExt() 在非盘中时段用 r[17] 接管美股显示（30分钟档），parseBatch 在非盘中直接跳过美股防止昨收覆盖；usSession() 用 America/New_York 时区判定，自动处理夏令时。标签显示"美股盘前(半小时档)"。用户桌面 Documents/Claude/Scheduled/verify-us-premarket-tencent 定时任务已无意义，可让用户删除。

## 区间表现榜（2026-07-07）
- 视图 "perf"（卡片在今日跌幅榜后）：一张卡装 5周期×2方向共10个榜。榜内周期胶囊(近1月/近3月/近半年/年初至今/近1年)+涨跌方向开关；状态存 localStorage(perfW/perfDir)，卡片副标题跟随周期；与市场过滤器联动；"上市后 x%"字符串行不参与排名；手机端胶囊横滑。

## 近1周窗口 + 超额榜两步框架（2026-07-07 晚）
- 行数据第19元素 r[18]=近1周涨跌（流水线 ts_1w）。列序：…当日→近1周→近1月…；SORT_MAP 5列→18；区间表现榜含近1周共6周期。追加字段一律 append 到行尾，禁止中间插位（避免全量下标漂移）。
- 超额榜两步框架（丹萍）：SEC_BENCH 板块正则→赛道指数（半导体系→SOX、软件→IGV、中国互联网→PGJ、加密→BTC、巨头/云→NDX，全是面板已有行）。第一列 vs赛道=个股alpha（排序键，无赛道基准者按vs大盘），第二列 vs大盘=市场超额；双正🏆双强；浮窗同步两步显示。A股半导体对标SOX为全球行业口径，已在说明注明。

## 修复与紧凑模式（2026-07-07 晚二）
- 教训：近1周上线时主表 rowHTML 替换静默未命中→表头11列/行10格错位，"近1年"看似消失。铁律：任何改列操作后必须校验「COLS数==rowHTML/fvRow的td数」（node校验脚本已用），python replace 后要 assert count==1，不许静默跳过。
- "上市后+x%" 不再占列宽：fmtPct 渲染为灰色"／"+title悬停（上市时间不满该窗口·上市至今+x%）。
- 紧凑模式：.tbl-wrap width:fit-content、table width:auto+min-width:100%，表格按内容收缩不再拉伸注水；th/td 水平内边距 9→13px。吸顶表头本就逐板块测量，自动适配。

## 填充列布局（2026-07-07 定版，用户方案）
- 表格布局最终形态：数据列内容自适应紧凑 + 行尾隐形填充列(th.fill/td.fill, width:100%,padding:0)吸收全部剩余宽度 → 卡片满宽与视图条对齐、行线贯通、右侧留白在框内。主表/榜单/热力全套；grp行 colspan=COLS.length+1。列数校验规则更新：rowHTML/fvRow td数 = COLS数+1(填充)。
- 上市不满窗口的"／"悬停改用 #stip 浮窗（.ipoflag[data-tip]，悬停即出+点击可固定），禁用原生 title/cursor:help（有延迟、手机无效、问号光标被用户否决过）。

## 四项增强（2026-07-07 晚三）
- 热力表周期切换：当日/近1周/近1月胶囊（data-hw，HEAT_W 存 localStorage heatW），中位/上涨家数/领涨/分化全随周期。
- 数据健康警示：静态数据工作日>4小时、周末>26小时未更新时，topbar 前插入金色警示条（现价实时层不受影响）。
- 亏损公司估值补位：流水线 v7 quote 顺带取 forwardPE→行尾 r[19]；mcapCell 无 PE 时显示"远期PE x"。行数据现为 20 元素(0-19)，新字段继续 append 行尾。
- 手机吸顶表头修复：topOf() 硬编码 56 改为实测 #header.offsetHeight（手机两行头 94px，浮头曾被压在头条下看不见）。

## 新会话交接指南（换窗口/换模型必读）
1. 让烟囱挂载文件夹「01_公司投研」。Claude 读本文件（us-stock-notes/CLAUDE.md）即可无缝接手；Codex 先读 `AGENTS.md`，再读本文件，不依赖旧对话记忆。
2. 推送凭证：01_公司投研/us-stock-notes 本地克隆的 origin 已内置 token（存在 .git/config，本地文件不会被提交）。直接在该目录 `git add/commit/pull --rebase origin main/push origin main` 即可，无需再要 token。⚠️ 当前 token（us-stock-notes-bot，含 Contents+Actions 读写）2026-10-06 到期。
3. token 到期续期（教烟囱操作）：github.com 右上头像 → Settings → Developer settings → Personal access tokens → Fine-grained tokens → Generate new token → Repository access 只勾 us-stock-notes → Permissions 里 Contents 选 Read and write → 有效期建议 90 天 → 生成后把 github_pat_ 开头的字符串发给 Claude，Claude 执行 `git remote set-url origin https://x-access-token:新token@github.com/chenyanchong321/us-stock-notes.git` 写回本地克隆。
4. 铁律：token 只放 .git/config（本地），绝不写进任何会被 commit 的文件（公开仓库，GitHub 扫到会自动吊销）。

## 新增/修改标的后必须立刻出数据（铁律，2026-07-07 补）
烟囱不等下一班 cron——改完 watchlist/profiles 推送后，必须马上手动触发流水线：
1. 沙盒连不上 api.github.com，但可以用 Claude in Chrome 浏览器工具操作网页触发：navigate 到
   https://github.com/chenyanchong321/us-stock-notes/actions/workflows/update-data.yml
   → 点右侧「Run workflow」下拉（坐标约 1304,418，以截图为准）→ 点弹层里绿色「Run workflow」按钮（约 1089,561）→ 截图确认出现新的 Queued run。
2. 若没有浏览器权限，就直接请求加载 Claude in Chrome 工具（ToolSearch: claude-in-chrome）；实在不行才告知烟囱去手动点，并给他上面的链接。
3. run 约 3-5 分钟；结束后 git pull 验证 data/quotes.json 里新标的行已生成（17+元素、近1周 r[18]、远期PE r[19]），再等 Pages 部署 1-2 分钟，用 ?v=随机数 访问验证页面可见。
4. Pages 部署偶发失败/CDN 缓存约10分钟：kick 空提交重试≤2次，然后等缓存。
5. 新增标的时顺带做三件事：profiles.json 简介（记忆卡风格）、已知的重要事件（业绩预告/财报日/上市日）写入 config/events.json、若烟囱给了买点则按"具体数值+锚定日期"写 buypoints.json。

## 锚点跳转防遮挡（2026-07-07）
- tr.grp/.section 的 scroll-margin-top 改为动态 CSS 变量 --anchor-grp/--anchor-sec（floatHead IIFE 的 setAnchors() 实时测算 = 顶栏+吸顶视图条+悬浮表头高度）。以后凡改变顶部吸顶层高度（视图卡增减、顶栏换行）无需手调数字。

## 沙盒 git 操作规范（2026-07-07 定，防锁冲突）
- 挂载盘的本地克隆（01_公司投研/us-stock-notes）只当"烟囱本地镜像"：新会话不要在里面做 commit/rebase/reset 等写操作（多会话并发会撞 .git/index.lock，且部分沙盒无删除权限清不掉锁）。
- 正确姿势：把仓库 clone/复制到自己会话的 outputs 临时目录里干活，push 到远端；镜像同步只用 `git pull --ff-only origin main`，遇到 index.lock 存在就跳过同步并告知烟囱（哪个会话有删除权限哪个处理，或烟囱手动删）。
- 镜像 origin 已内置 token（.git/config），临时副本可从镜像 config 里读 remote URL 拿到推送地址。

## 回复语言（2026-07-07 定，烟囱明确要求）
- **一律用中文回复烟囱**：不只是最终答复，连思考/推理过程也用中文，禁止夹带英文整句（如 "Got it, the updated rules…"）。烟囱看不懂英文思考过程。
- 例外：代码、命令、专有名词、股票代码、API 字段名等技术标识按原文保留即可；术语缩写首次出现按烟囱偏好给「英文全称（中文译名）」。
- 这是跨会话固定规则，任何新会话接手后默认遵守，无需烟囱再次提醒。

## 长开页面数据自动跟班（2026-07-07 深夜）
- refetchStatic()：非盘中时段每10分钟重拉 data/quotes.json 原地更新 DATA 并重应用盘前/盘后价（applyExt），解决"页面开着不动、盘前价停在打开那一刻"的问题。盘中由30秒实时层负责，不重拉。

## ADR/OTC 实时行情修复（2026-07-07 深夜，系统性bug）
- 教训：txCode 只匹配"美股/美股 ETF"，"美股 ADR/美股 OTC"标签的标的（NOK/ASML/HIMX/TSM/ARM/KXIAY等一批）从未接入30秒实时，盘中显示半小时档旧价被用户发现。已改为 mkt.indexOf("美股")===0 全覆盖；腾讯没有的代码（个别OTC）自然落空无副作用。
- refetchStatic 盘中也重拉（重拉后立即 refresh() 盖回实时价），保证无实时覆盖的残余标的至多滞后一班cron。
- 铁律：新增市场标签类型时必须检查 txCode 是否覆盖，验证方法=盘中抽查该标签下标的的"实时 HH:MM:SS"是否跳动。

## 现价悬停改买点卡（2026-07-08，用户定版）
- 52周位置浮窗移除（与现价第二行的52周区间重复）；posTip 改为买点卡：有买点的标的悬停现价弹「买点原文＋现价＋距买点差距/已触达」，无买点的不弹。价值：买点信息跟着搜索走，不再只锁在重点清单。

## 数据滞后警示收紧（2026-07-08）
- GitHub Actions cron 会成班跳过（今天下午连跳5班，免费档通病，无法根治，手动 Run workflow 兜底）。警示条阈值：工作日北京8-24点 1.5小时，其余工作日时段 4小时，周末 26小时。跳班时页面自动挂黄条，不依赖用户对照券商发现。

## 阿里云ECS定时闹钟（2026-07-08 上线，根治GitHub cron跳班）
- 架构：阿里云ECS(118.31.109.150, 杭州, Asia/Shanghai时区) crontab 通过 GitHub API workflow_dispatch 触发 update-data.yml，替代不可靠的GitHub自带cron（后者保留为备胎，两个闹钟谁响都能开工，仓库有并发防撞）。2026-07-09 起，美股慢变流水线改为每15分钟；A股/港股时段保持每30分钟(7,37)。
- 脚本 /root/us-stock-trigger.sh 读 /root/.gh_token（600权限，仅root可读）+ /root/.gh_payload（{"ref":"main"}）；日志 /root/us-stock-trigger.log。cron: `*/15 16-23 * * 1-5` + `*/15 0-8 * * 2-6` + `7,37 9-15 * * 1-5`。
- 首次验证 dispatch=204 成功（2026-07-08 20:55）。
- token 由用户亲手粘贴进服务器（安全classifier禁止AI把明文token写文件，此为必经人工步骤）；token续期(2026-10-06)时需用户重新执行那段命令更新 /root/.gh_token。
- 探针发现（备用增强，未实施）：ECS带Referer访问 hq.sinajs.cn/list=gb_XXX 可拿美股秒级实时盘前价（DRAM/MU实测成功），可做盘前分钟级直通车；ECS能连api.github.com但连不上GitHub Pages(国内被墙确认)。

## 美股盘前/盘后数据源的踩坑史（2026-07-09 补）
- 腾讯：美股非盘中时段是延时冻结口径，不可用。
- 新浪 `gb_`：盘前只返回昨收（实测 143 只里 137 只与昨收完全一致）。已弃用。
- Yahoo：数据正确，但从阿里云 ECS 直连会被 403；只能由 GitHub Actions 流水线取（6~15 分钟档）。
- Nasdaq `api.nasdaq.com`：ECS 可直连，是当前 `live.json` 的实际数据源（分钟档）。两个已知缺陷：(a) 不覆盖 OTC 与指数；(b) 个股盘前无成交时会退回常规时段数据（价=昨收、涨跌幅=昨日的）。
- 前端因此有两道防呆守卫（`applyLive()` 内）：整批守卫（>70% 等于昨收则整批丢弃）+ 逐只守卫（单只等于昨收则跳过）。若将来换源后页面价格不动、console 出现 `疑似昨收快照`，是数据源在盘前不给盘前价，去查字段，不要动守卫阈值。
- 2026-07-09 收尾：`live.json` 写入 `src`/`n` 方便排查；Yahoo 失败后用 `/root/.yahoo_cooldown` 冷却 1 小时，避免 ECS 每分钟白撞 403；美股流水线触发频率从 6 分钟放慢到 15 分钟，A股/港股时段保持 `7,37` 不变。

## 回撤台阶功能 + 买点浮窗美化（2026-07-08）
- 买点浮窗(posTip)买点数字改金色加粗(var(--gold))，醒目。
- 回撤台阶：ladderOf(r) 从历史高点算 −30%~−80% 六档价格。①历史高点列(td.athc)悬停弹台阶表(athTip)，当前档金色▸高亮、已跌破档灰色、每档带"距现价还需±%"；②新增视图 "ladder"(🪜回撤阶梯)全表横向对比，卡片在新高榜后。三个悬停位分工：名称=简介、现价=买点卡、历史高点=回撤台阶。
- ladderOf 用 parseCur/parseNum 处理多币种前缀($/HK$/¥/₩)。

## 回撤台阶：现价档主次对调 + 买点插入（2026-07-09）
- 「历史高点/距高点」列改为「回撤/ATH」：回撤大字在上、ATH小字在下（主次对调，用户最关注跌幅）。
- 回撤台阶浮窗(athTip)两列制（跌幅左/价格右），插入三类档：金色=现价(现价)、绿色🎯=建议买点(买点①②③)、灰白=固定−30~−80刻度；已跌破档灰显。买点价格从 BUY[code] 解析（取"（"和"·"前的数字，排除日期/市值锚注释），按跌幅换算插到正确位置。
- 吸顶表头修复：html,body 用 overflow-x:clip（不建立滚动容器，不破坏 sticky/floatHead），替代之前的 overflow-x:hidden。
- 新增：宁德时代H股 03750（价值老登/储能组，买点600左右）；宁德A买点改330-350；Circle CRCL买点由"市值$100亿"换算为"37以下"(跌87.5%)以便台阶绘制。

## 市值排行榜（2026-07-12 增）
- 新榜单卡 data-view="mcap"（💰市值排行榜，紧跟「市场·全景」卡），点击进入不分板块、不分组的纯市值降序清单。
- 市场范围完全跟随「市场·全景」全局筛选（flatRows 内建 mktOk），榜内不再设市场按钮；标题动态显示「全市场/A股/港股/…」。
- 排序键 = mcapVal()（本币市值折算亿美元，FX2USD 粗略汇率，只影响排名不影响展示）；剔除 指数/ETF/商品/外汇，加密资产参与（BTC 全市场约第10名）。
- 列序：# 排名 → 名称 → 市值（主人指定放第二列）→ 现价/1年 → 当日/1周 → 回撤/ATH → YTD → 1年。市值列复用 mcapCell（本币+美元两行、悬停PE浮窗）。
- **无 TopN 筛选**（主人指定，2026-07-12 定稿）：始终显示完整榜单，理由是"想看前30往下拉30行就行，完整榜单天然包含所有 TopN"。曾做过 Top30/50/100 药丸后被移除，勿加回。卡片角标固定「全表」，不写 TopN（避免误以为榜单只收录前30名）。
- **榜内市场快捷切换**（2026-07-12 增）：榜单下方一排 perf-pill（全部/A股/港股/美股/日股/韩股/台股/加密，带参与排行的标的数），data-mkt 直接调 window.setMkt → 与「市场·全景」是同一个全局开关（主人拍板：单一市场状态，不做榜内独立筛选），切完全景卡文字同步变、退出榜单后面板也保持该市场。
- 注意：A/H 两地上市（如宁德时代）在全市场视图下 A、H 各出现一行（全站按代码分行的既有逻辑），说明文案已标注。

## 市场规模锚点自动校准（2026-07-12 上线，消灭人工季度校锚）
- `scripts/update_anchors.py`：每周自动用官方源重校 config/marketscale.json 的绝对锚点。港股=HKMA开放API（市值+月末恒指官方配对，质量最高）；日本=JPX官方Excel historical-jika.xlsx（固定URL月更）；A股=上交所commonQuery+深交所ShowReport（亿元，需Referer）；韩国=KRX数据门户MDCSTAT00101（코스피+코스닥 MKTCAP）；美股不动绝对锚、仅探测把浮动指数升级为Wilshire全市场指数。
- 防呆三件套：各市场万亿美元 SANITY 区间出界即拒收；单位靠 infer_unit_to_usd_t 量级推断（JPX/KRX单位未实测坐实，靠推断兜住）；任何失败保留旧锚+逐市场诊断写 data/anchors_log.json（远程排障看这个文件，不用翻Actions日志）。
- `.github/workflows/update-anchors.yml`：周日22:23+周一4:23 UTC双班（抗cron跳班，脚本幂等），改脚本push即跑（验证用）。工作流内默认token提交不会连锁触发update-data，等下一班行情流水线自然生效。
- 若 A股/韩国源在美国runner长期不通：改由杭州ECS抓（它连中国源零障碍、写GitHub通道已有），方案见求助文档第九节。

### 锚点校准实测终局（2026-07-12）
- 自动直抓成功：港股HKMA、日本JPX、美股^W5000浮动指数迁移。失败判死：深交所对海外IP软拒（连通但空表）、KRX需登录账号 → A股/韩国/美股绝对锚由主人桌面端Cowork定时任务 verify-marketscale-anchors（每月3日）搜索核对，偏差>3%自动改config并推送。
- 排障入口：data/anchors_log.json（每轮校准的逐市场诊断）。A股若将来要日更官方数：ECS抓沪深两所→GitHub API写回（通道已有）。

### A股锚点最终路线：ECS哨站（2026-07-12 定稿，替代AI月检）
- 主人拍板：不用桌面定时任务（依赖电脑开机，违背全自动原则），改用阿里云ECS哨站。
- ECS /root/us-stock-cnmcap.py（安装说明：交接给ECS_A股市值哨站.md，主人粘贴一次）每周一/四18:07抓沪深两所官方总市值→GitHub API写回 data/cn_mcap.json；update_anchors.py 优先读此文件（≤10天有效），失效才尝试直抓。
- 韩国绝对锚：接受每年2-5%漂移（KOSPI浮动全自动，卡片是量级展示看不出）；美股靠^W5000全市场指数自解决。桌面AI月检任务已删除。

### ECS 可由 Claude 直接操作（2026-07-12 实证，重要）
- 路径：Claude in Chrome 扩展 → 阿里云控制台（主人常年已登录）→ ECS 实例详情 → 远程连接 → Workbench **免密连接已启用**（root 免密码登录）→ 网页终端里直接敲命令。
- 以后凡"需要主人往服务器粘贴"的操作，一律 Claude 自己走这条路，不再麻烦主人。哨站安装（2026-07-12）即由 Claude 全程自操作完成：一行 `curl api.github.com/...ecs_install_cnmcap.sh | bash`。
- A股哨站闭环实证：ECS抓沪669808+深468713=1138522亿元 → GitHub → 校锚工作流 → A股锚 16.83T @ 沪深300 4842（源标注"ECS哨站(沪深交易所官方)"）。旧人工锚16.5T存在约2%漂移，已被自动修正。

## 榜单市场切换全站化 + 角标联动（2026-07-12）
- mktBarHTML()：市场快捷切换条（全部/A股/港股/美股/日股/韩股/台股/加密，带标的数）追加到**所有**榜单视图（含重点清单/回撤阶梯/板块热力）的说明下方，data-mkt → setMkt，与「市场·全景」同一全局开关。样式复用 #perfbar（选择器 #perfbar,#mktbar）。
- updateBadges()：数量类角标随市场联动——st-focus重点清单/st-deep回撤榜/st-hi新高榜/st-mcap市值排行榜(显示"市场名 数量")/st-heat板块热力。setMkt 与初始化时调用。规则类角标（Top30/近1年/全表）不动（主人确认的分类）。

## 榜单列序原则：排序依据紧跟名称（2026-07-12，主人定的通用原则）
- **用户点进哪个榜，该榜的排序依据列就放第二列**（名称之后）——市值排行榜→市值第二列；回撤榜/新高榜→「回撤/ATH」第二列（fvRow/FV_HEAD 的 athFirst 参数）；超额榜维持原样（主人明确）。以后新增榜单遵循此原则。
- 榜单视图列序可安全调整：实时刷新的 setHTML 逐格补丁只作用于 #sections 主表，flat 视图一律整体重建（buildFlat），不会串位。

## 📜 金融史页签（2026-07-12 上线）
- 第五页签「📜 金融史」：与大事件互为镜像（那边未来，这边过去）。config/history.json = 16篇编年档案（1602股票诞生→2022加息熊市，crisis红/milestone金），字段 story/chain/stats(drop·dur·heal)/policy/lesson，记忆卡文风。**铁律：再发生历史级危机/里程碑，往 events 末尾追加档案（含 mark=YYYY-MM 走势图标注）。**
- 页面 = 标普500百年对数走势图（data/spx_history.json，update_anchors.py 周更产出 ^GSPC max月线；缺失时只显示时间轴）+ 竖向编年时间轴。图上红点/金点可点击直达档案卡（hflash高亮）。渲染函数 renderHistory()。
- 内容为静态历史事实（跌幅/修复年数为公认口径），无需流水线伺候。

### 全景视图横向市场切换条（2026-07-12 补齐）
- #mktbar-all（视图卡行下方、机会条上方）：全景主视图与各榜单一致的横向市场按钮，refreshMktBarAll() 复用 mktBarHTML 按钮段；setView 控制显隐（仅全景），setMkt 刷新选中态。mktbar 由 id 改 class（全景/榜单两处并存）。至此全站每个视图都有横向市场切换。

## 市场切换条终版：双行胶囊+权重排序+全景卡直切（2026-07-12 定稿）
- 胶囊顺序=主人定的权重序：全部/美股/A股/港股/日股/韩股/台股/加密（美股最前，手机不用滑）。
- 双行胶囊(.perf-pill.mk2)：第一行 市场 数量 ●盘中(绿)/○休市(灰)，第二行 交易时段（mktStatus，北京时间，美股自动判夏令时；盘中整行绿，加密=7×24）。「全部」第二行显示"N 市盘中"。每60秒 setInterval 刷新两处切换条。
- 「市场·全景」卡=纯开关：点击直接回全景+重置全部（与其他视图卡一点即达对齐）；mktmenu 下拉菜单已整体删除（开盘信息移入胶囊）。保留筛选回全景的路：再点当前榜单卡。

## 措辞去风险化：买点→观察位（2026-07-12 主人定，铁律）
- 用户可见文案一律不出现"买点"：已进买点区→**已进观察区**、买点 xx→**观察位 xx**、距买点→距观察位、回撤台阶浮窗 买点①②③→观察位①②③；重点清单说明补"观察位为个人学习记录，不构成投资建议"。
- 内部命名不动：config/buypoints.json、BUY/buyTh/buyLabel 等变量、代码注释照旧。**以后新增任何前端文案禁用"买点"字样，统一"观察位"。**《投资机会和价格点位》文档同步时也注意换词。

## 涨跌幅榜三合二（2026-07-12 定稿）
- 今日涨幅榜/今日跌幅榜/区间表现榜 → 「📈涨幅榜 Top30」+「📉跌幅榜 Top30」两张卡。方向由卡片决定（换方向=点另一张卡，榜内无方向开关）；榜内周期胶囊：今日/近1周/近1月/近3月/近半年/年初至今/近1年。
- **每次点进都重置为「今日」**（PERF_W 不再 localStorage 记忆，主人定）；角标 st-gain/st-loss 同步显示当前周期。
- 列序原则（主人定稿，本榜专用变体）：**名称/现价 锚定前两列，选中周期列第三**，当日/回撤/市值不动，市值后周期列按时间序且跳过已前置的（fvRow/FV_HEAD 的 skipRi 参数）。「今日」= 默认布局零变化。
- PERF_DIR/perfW/perfDir/st-perf 已全部移除，勿复用。

## 日韩盘中延时实测（2026-07-13，补齐2026-07-10留的"未实测"）
- 方法：盘中经 qt.gtimg.cn 拉 jp7203/jp6501/kr005930/kr000660，读 f[30] 时间戳（北京时间口径）对比墙钟，三轮采样（12:34/12:37/12:40）。
- 结论：**日股延时约15-20分钟，韩股约20-25分钟**，且非平滑延时——约5分钟一档快照跳动（时间戳每5分钟跳一格，间隔内冻结）。价格有效（三星258500→259000在动）。
- 已写进 refresh-note 正文与 tooltip。台股仍未实测（前端实时层本就不覆盖台股）。
- **同日第二轮独立复测（13:02-13:07，方法互补）**：① **A股实时确认（秒级）**——复盘后快照价与自家分时最新一分钟精确相等（宁德时代 358.39↔1303、358.98↔1307，紫金矿业同），时间戳落后墙钟仅2-4秒；② 日/韩用 Yahoo 1分钟线反查快照价，与 f[30] 时间戳法吻合（日 15.9/17.3/18.7 分、韩 20.8/22.2 分），且证实 Yahoo 自己日股延时15分钟、韩股20分钟，不能当实时源；③ **台股已实测：腾讯无台股行情**（tw2330/tw02330/tw0050 均返回 v_pv_none_match）；④ 港股复验——午休开始15分钟后快照时间戳恰冻结在 11:59:59，与15分钟延时精确一致。详见《数据源架构说明.md》三节实测表。

## 回撤榜成对改造（2026-07-13 主人定稿）
- 「🚀新高榜(<10%)」→「🚀回撤榜(<30%)」：与「回撤榜(>30%)」成对，同涨/跌两卡逻辑（来意定于卡片，点击即达，无二次选择）。视图代号仍为 hi，阈值 r[6]>-30。
- 排序原则=**每张榜把自己的极端放头条**（非数字统一升降序）：>30%榜跌最深在上；<30%榜离新高最近在上（头部即原新高榜内容，老习惯无缝衔接）。角标 st-hi=当前市场 <30% 标的数。

## 导航与卡片双收纳（2026-07-13 主人定稿）
- **顶部页签**：只留 数据面板/学习笔记/☰更多（桌面手机统一）。大事件/术语词典/金融史收进 #tab-more 下拉（#moremenu）；**以后加新栏目（出书/联系作者等）一律进「更多」菜单**。所有切页签走 window.switchTab(v)，勿再 .tab.click()。
- **视图卡**：一级7张=全景/重点清单/涨幅/跌幅/回撤>30%/回撤<30%/市值排行榜 + 「📦其他」抽屉卡；超额榜/回撤阶梯/板块热力/市场规模带 .xtra 类收进抽屉（点其他展开/收起，进入抽屉内视图自动展开+入口高亮，ensureXtra/XOPEN）。
- **手机端市场规模弹层**修复：≤860px 时 #scalemenu 改 position:fixed 左右贴边+65vh限高，杜绝溢出屏幕。

### 手机端更多下拉修复 + 侧边栏板块热力（2026-07-13）
- 手机端 .tabs 是 overflow-x:auto 横滑容器，会裁剪 #moremenu 下拉（"点更多没反应"根因）→ ≤860px 时 #moremenu 改 position:fixed 贴顶栏右侧。凡在滚动容器内做下拉/弹层，手机端一律考虑 fixed 脱离裁剪（scalemenu 同款教训）。
- 侧边栏目录每个板块名后加 .navheat（当日中位涨跌，绿涨红跌），口径与板块热力图一致（个股、剔ETF/指数、随市场筛选联动），updateNavHeat() 由 updateBadges+60s interval 驱动。主人定位：目录=固定座次的热力条，热力图=动态排名，分工互补。

## 术语速查抽屉（2026-07-13 主人定）
- #dictdrawer：右缘固定把手「📖术语」，桌面悬停展开/移开收起（搜索框聚焦时不收）、手机点击开合，全站各页签可用。搜索复用 window.GLOSSARY（config/glossary.json），匹配 t/f/d 字段，最多显示15条卡片。
- 设计哲学：词典页管"逛"（系统浏览），抽屉管"查"（point of need），与标的简介浮窗同一原则。新增词条自动进抽屉，零额外维护。

### 词典右侧栏（2026-07-13 终版，替代速查抽屉）
- #dictbar：桌面=右缘细条(26px)悬停展开为完整侧栏（搜索+分类胶囊+A-Z字母索引+全量词条卡），展开时全站容器 padding-right:var(--rdict) 让位（Askwise三栏）；📌钉住常开(localStorage rdictPin)、左缘拖拽调宽240-440(rdictW)、»收起。手机=居中悬浮抽屉不挤压。
- A-Z 归类与词典页同源（非字母开头归「中」），分类/字母/关键词三重过滤叠加。词条数据 window.GLOSSARY。

### 词典侧栏交互终版（2026-07-13 二修）
- **点击展开**（悬停只高亮不弹出——悬停展开有"收起后细条出现在鼠标下→mouseenter→秒弹回"的死循环bug，且右缘要留给未来挂件，勿改回悬停）。
- »收起永远有效（含钉住态，同时解除钉住）；📌钉住=金色实底强反馈+持久化；未钉住时点面板外可关，钉住时只认»。

## 现价浮窗均线支撑（2026-07-13 主人需求上线）
- 行数据 r[20] = [MA20,MA50,MA60,MA120,MA200]（update_quotes.py 复用5年复权日线顺手计算，零额外请求；不足N日为null；失败行补null）。追加铁律照旧：行尾追加，勿插位。
- 前端 maBlock(r)（posTip 内）：五行均线卡——名称+白话身份（月线·短期趋势/50日·美股中期生命线/季线·A股港股中期/半年线·中长期分水岭/年线·牛熊分界）+价位+与实时现价的距离；绿=站稳/红=已跌破/灰=数据不足；底部自动形态总结（全站稳=多头/全破=空头/部分破=列出已破线+指出下方支撑线）。观察位/52周/美元换算原内容保留在上方。
- 周线级短均线（MA5/MA10）主人明确不要（短线噪音）。MA20=月线行业惯例（非MA21）；年线统一MA200（跨市场可比，A股250差异忽略）。

### 均线台阶浮窗定稿（2026-07-13 三版收敛，勿再改）
- 排版=台阶排位（均线按价格降序，▶现价金色插入实际位置）+ 每行明亮显示「已跌破 −x%」红/「在上方 +x%」绿（幅度=现价相对该线）。灰色只留给"数据不足"。
- 原则（主人定）：位置管"现价在哪"，行内红绿幅度管"距离与性质"，每个视觉元素只说一件事；红绿严格对齐全站绿涨红跌语义（禁止绿色配负号）。

### iOS 输入框防自动放大铁律（2026-07-13 二次踩坑后定稿）
- iOS Safari/微信内嵌：输入框 font-size<16px 时聚焦自动放大页面且不回弹。**全站任何输入框手机端(≤860px)字号一律≥16px**。已修：#search(此前已修)、#db-q(词典侧栏)、#dict-search(词典页15px漏网)。新增输入框必查此项。
### 浮窗表格三列对齐惯例（2026-07-13 主人认可沉淀）
- 名称列左对齐+nowrap、数字列右对齐（个位对齐可比大小）、状态列右对齐。全站浮窗/小表格一律照此，禁止依赖全局td居中默认值。

## 悬浮市场快捷dock（2026-07-14 主人需求上线）
- #mktfloat：底部居中悬浮dock（避开顶部三层吸顶+手机拇指热区），单行紧凑胶囊（市场名+数量+●○），点击=setMkt全局切换。
- 出现逻辑：顶部切换条(refBar=当前视图的.mktbar)滚出吸顶区(bottom<100)后——桌面常驻浮现；手机需叠加"上滑意图"(dy<0)，下滑阅读自动隐藏（iOS工具栏范式）。仅数据面板视图生效。
- setMkt 会刷新dock选中态(refreshMktFloat)。注意手机端 #viewbar 是 static 不吸顶（历史设定），dock是手机切市场唯一的免回顶通道。
